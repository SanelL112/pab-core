[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-config --strict-markers
required_plugins =
    pytest-asyncio>=1.0
    pytest-mock>=3.0
markers =
    asyncio: run a coroutine test with pytest-asyncio
norecursedirs = .git .pytest_cache venv scratch

