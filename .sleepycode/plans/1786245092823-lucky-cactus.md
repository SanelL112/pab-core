# Plan: Generate CODEBASE_ANALYSIS.md for personal-assistant-bot

## Objective
Produce `CODEBASE_ANALYSIS.md` (at repo root) — a deep architectural analysis of
this Telegram-based personal-assistant bot, following the user's required
6-section Markdown structure. This is a documentation/read-only-analysis task;
the only file created is the deliverable.

## Evidence gathered (first-hand, verified)
- `README.md`, `AGENT_BRIEFING.md`, `AI_CONTEXT.md`: intent + operator rules.
- `config.py` (249 lines): env-driven settings, `PAB_*` root overrides, all state
  file paths, model policy (`OR_DEFAULT/FALLBACK/THIRD`), RPC cluster sizing
  (`RPC_SURFACE_TIMEOUT` < `RPC_INFERENCE_TIMEOUT` with 60s reserve),
  `initialize_runtime()` (0o700 dirs, 0o600 secrets), `validate_runtime_config()`.
- `llm_router.py` (1619 lines): `ProviderTrust`/`Sensitivity`/`InferenceStatus`
  enums, frozen `InferenceResult`, `_cloud_policy_allows` (fail-closed; unknown
  sensitivity → PERSONAL, unknown provider → CLOUD), `call_openrouter`
  (OpenRouter→Opencode Zen→Hack Club fallback, deadline budgeting, streaming),
  `call_ollama` (Orange-Pi auto-route w/ local retry), `call_agy_local`
  (subprocess to `agy` CLI, Gemini aliases), `call_local_rpc` /
  `call_llamacpp_rpc[_with_fallback]` (Surface 10.0.0.47:8080 → Pi → Dell →
  gated cloud), `check_rpc_memory_ok` (Surface RAM veto, workers observability
  only), atomic `save_cost_log`, heuristic `is_valid_response`.
- `main.py` (1017 lines): handler registration, JobQueue schedules (nightly 1AM,
  morning 7AM ET), digest task extraction → Notion sync w/ fuzzy dedup
  (SequenceMatcher > 0.8, bounded `seen_tasks`), `digest_lock`/`watchdog_lock`,
  sleep-window gate (1–7AM ET), `@require_auth` on all handlers.
- `bot/` package: `security.py` (`require_auth` — private chat + owner user-id,
  reads `TELEGRAM_OWNER_USER_ID`), `state.py` (transactional `update_state`
  around `AtomicJSONStore`, `WeakValueDictionary` per-user asyncio locks,
  `is_sleep_window`, seen-list pruning dropping legacy MD5 digests),
  `storage.py` (`AtomicJSONStore`), `ai_bridge.py` (`send_to_antigravity_and_wait`
  — routes via semantic retrieval + local RPC result, explicit cloud consent,
  never leaks private corpus), `commands.py`, `ui.py`, `dashboard_state.py`,
  `runtime.py` (background task tracking).
- `scrapers/`: `memory_consolidation.py` (nightly phases: embed bootstrap,
  curated-brain compress, offline index, embedding rebuild + cache invalidate),
  `embedding_indexer.py` (chunk 1000 tok/200 overlap, nomic-embed-text dim=768,
  MD5 incremental manifest, atomic `.npz` staging, zero-row validation),
  `semantic_retrieval.py` (normalized cosine top-K via matmul, LRU index cache,
  `get_fallback_context` tail-read), plus google/canvas/notion/groupme scrapers,
  `mega_study_builder`, `nightly_processor`, `assignment_calendar`, etc.
- `utils.py`: `scrub_pii`/`check_pii` (email/phone/SSN/CC/DOB/student-id/IP/home
  regex → safe tokens), `run_bash_safely` (allowlist + audit), rotation, backups,
  correlation graph.
- Ops: `bot.service` — hardened systemd unit (NoNewPrivileges, ProtectSystem=full,
  PrivateTmp, RestrictAddressFamilies, after tailscale-online).
- Tests: 20 files (`pytest.ini`: pytest-asyncio, strict markers), incl.
  `test_p0_security.py`, `test_routing_privacy.py`, `test_rpc_budget.py`,
  `test_state_and_groupme_hardening.py`, `test_reliability*.py`.
- `requirements.txt`: python-telegram-bot[job-queue]==22.8, canvasapi, selenium,
  google-api-python-client/oauth, httpx, requests, pytesseract, Pillow, numpy,
  python-docx, bs4, ddgs, youtube-search/transcript, pdf2image, PyPDF2, radicale,
  bcrypt.

## Deliverable structure (matches required template exactly)
`# Codebase Overview & Architecture`
1. Executive Summary — purpose, runtime, key tech.
2. Directory & Component Structure — file map + module responsibilities.
3. Data Flow & Core Workflows — request path, nightly pipeline, calendar, backups.
4. Deep-Dive & Architectural Reasoning — security model, inference routing/FoR,
   state mgmt, RAG, design patterns, why decisions were made.
5. Dependencies & Configuration — packages + env/config mgmt.
6. Observations & Technical Considerations — bottlenecks, security, tech debt.

## Key analytical through-lines to convey
- **Privacy-by-design / fail-closed egress** is the dominant theme (defense in
  depth: PII filter → classification → consent → scrub).
- **Two-layer codebase**: hardened `bot/` package over legacy flat modules —
  incremental refactor-in-place; some duplication/drift results.
- **Self-hosted, resource-constrained** engineering (i5, ~5.7GB RAM): heavy work
  pushed to nightly batch + remote RPC cluster; strict token/timeout budgeting.
- **Files-as-database** with atomic JSON store, rotation, fuzzy dedup.

## Steps
1. Write `CODEBASE_ANALYSIS.md` with all six sections, grounded in the verified
   facts above (cite file:line where load-bearing). ~350–450 lines.
2. Verify: Markdown renders (headers/lists), all 6 required sections present,
   file/line references accurate.

## Verification
- `ls CODEBASE_ANALYSIS.md` exists; `grep -c '^## ' CODEBASE_ANALYSIS.md` == 6.
- Spot-check 2–3 cited symbols exist (e.g. `bot/security.py:34 require_auth`).
- No source code modified; only the new `.md` added.
